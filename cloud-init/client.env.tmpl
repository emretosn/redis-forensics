# client.env — non-secret connection details for querying redis-vm.
# NO passwords are stored here (Option A): each user authenticates with their
# own Redis credentials at runtime. Placeholders (__*__) are substituted by
# main.bicep at deploy time.
REDIS_HOST=__REDIS_HOST__
REDIS_PORT=6379
