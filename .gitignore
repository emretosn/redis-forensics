# SSH keys / collector keypair — never commit
secrets/
*.pem
*.key
id_rsa*
*_rsa
*_rsa.pub
collector_key*
pull_key*

# Bicep build output
*.json
!*.bicepparam.json
main.json

# Local parameter overrides with secrets
*.local.bicepparam
*.secrets.*

# Editor / OS
.DS_Store
*.swp
*~
.vscode/
.idea/
